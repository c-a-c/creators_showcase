# Contest Apps

This directory hosts individual contest submissions that are deployed as separate Vercel projects. Add each work under its contest year, for example:

```
apps/contest/2025/my-nextjs-game/
apps/contest/2025/my-static-art/
```

Each subdirectory should contain everything needed to deploy the work independently (its own `package.json` for framework apps or static assets for plain HTML works).
